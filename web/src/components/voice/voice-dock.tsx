"use client";

/**
 * STUB — replaced by the voice agent (task #4) with the real docked voice
 * widget (connect/mic/visualizer/transcript) fixed to the bottom of the screen.
 *
 * Keep this export name and path stable: it is mounted in `app/layout.tsx`.
 * Returns null for now so the standalone app renders cleanly without voice.
 */
export function VoiceDock() {
  return null;
}
